{
  "name": "packet-lab-manager-server",
  "version": "1.0.0",
  "description": "Packet Lab Manager Server — Windows (C# worker) & Linux (native) dual-mode",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "switch:deep-report": "node tools/switchDeepReport.js",
    "install-native": "npm install serialport cap --save-optional"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "serialport": "^13.0.0",
    "ws": "^8.16.0"
  },
  "optionalDependencies": {
    "cap": "^0.2.1"
  },
  "engines": {
    "node": ">=22 <23"
  }
}
